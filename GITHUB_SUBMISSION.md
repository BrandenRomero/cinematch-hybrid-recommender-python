# GitHub submission

Suggested repository name:

```text
cinematch-hybrid-recommender-python
```

Suggested link to submit after pushing:

```text
https://github.com/YOUR_USERNAME/cinematch-hybrid-recommender-python
```

Quick push commands:

```bash
git init
git add .
git commit -m "Final Python CineMatch recommender project"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/cinematch-hybrid-recommender-python.git
git push -u origin main
```
